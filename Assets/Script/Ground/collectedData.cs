using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class collectedData : MonoBehaviour
{
    /*private void OnCollisionEnter(Collision collision)
    {
        if (collision.gameObject.CompareTag("Ground"))
        {
            if (!Collector.col.colliderList.Contains(collision.gameObject))
            {
                collision.gameObject.tag = "Grounded";
                collision.transform.parent = Collector.col.grounded;
                collision.gameObject.AddComponent<groundBlock>();
                Collector.col.colliderList.Add(collision.gameObject);
            }
        }
    }*/
   
}
